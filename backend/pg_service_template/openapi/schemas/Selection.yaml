type: object
properties:
    id:
        type: string
        format: uuid
    name:
        type: string

    description:
        type: string

    picture:
        type: string

    link:
        type: string

    pre_created_collection_name:
        type: string
        enum: ['Хочу сходить', 'Хочу заказать']

required:
    - id
    - name
    - description
