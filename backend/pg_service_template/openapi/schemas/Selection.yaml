type: object
properties:
    id:
        type: string
        format: uuid
    name:
        type: string

    description:
        type: string

    is_public:
        description: "Enum размером в 2 байта"
        type: integer

required:
    - id
    - name
    - description