post:
    description: "Получить рекомендация по заведениям в определенной области"

    requestBody:
        required: true
        content:
            application/json:
                schema:
                    type: object
                    properties:

                        lower_left_corner:
                            description: "Координаты левого нижнего угла"
                            $ref: "../schemas/index.yaml#/Coordinates"

                        top_right_corner:
                            description: "Координаты правого верхнего угла"
                            $ref: "../schemas/index.yaml#/Coordinates"

                        filters:
                            type: array
                            items:
                                $ref: "../schemas/index.yaml#/RestaurantFilter"

                    required:
                        -   lower_left_corner
                        -   top_right_corner


    security:
        -   UserTicketAuth: [ ]


    responses:

        200:
            description: "OK"
            content:
                application/json:
                    schema:
                        type: object
                        properties:
                            items:
                                type: array
                                items:
                                    $ref: "../schemas/index.yaml#/Restaurant"

        400:
            $ref: "../responses/400.yaml"
        401:
            $ref: "../responses/401.yaml"